INPUT:
{
            sum = a.x + b.y;
            product = data.val * count.num;
            diff = first.coord - second.coord;
        }

OUTPUT:
{
  sum = (a.x + b.y);
  product = (data.val * count.num);
  diff = (first.coord - second.coord);
}
