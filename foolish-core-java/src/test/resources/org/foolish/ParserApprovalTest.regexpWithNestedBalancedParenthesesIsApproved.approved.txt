INPUT:
{
            result = brane.((nested));
            deep = data..{outer[inner(deep)]};
            mixed = x.(a{b[c]});
        }

OUTPUT:
{
  result = brane.((nested));
  deep = data..{outer[inner(deep)]};
  mixed = x.(a{b[c]});
}
