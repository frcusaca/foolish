INPUT:
{
            sum = a.b + c.d;
            product = a.b * c.d;
            complex = -a.x + b.y * c.z;
            nested = (a.b + c.d) * (e.f - g.h);
        }

OUTPUT:
{
  sum = (a.b + c.d);
  product = (a.b * c.d);
  complex = (-a.x + (b.y * c.z));
  nested = ((a.b + c.d) * (e.f - g.h));
}
