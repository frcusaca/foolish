{
  y = { a = 1; b = 2; a = 3; };
  z = y . a;
  z_question = y ? a;

  y2 = { abc = 10; bcd = 20; };
  z2 = y2 . (b.*);
  z2_question = y2 ? (b.*);

  z3 = y2 . (^a);
  z4 = y2 . (^a.*);

  y3 = { type'x = 100; other'x = 200; };
  z5 = y3 . type'x;

  !! Partial match with anchor
  y4 = { abc = 500; };
  z7 = y4 . (^a);
}
