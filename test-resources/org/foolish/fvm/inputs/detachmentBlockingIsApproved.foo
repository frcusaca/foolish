[α = ???; β = ???]{
	result = α + β;

	!! Blocking cascades to nested branes
	nested = {
		x = α;
		!! α should be blocked here too
	};

	!! Redundant blocking - blocking twice should work
	redundant = [α = ???]{
		!! α blocked again at this level
		y = α;
	};

	!! Deeply nested blocking cascade
	deep = {
		level1 = {
			level2 = {
				!! α and β blocked all the way down
				z = α + β;
			};
		};
	};
}
