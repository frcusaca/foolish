name: Java Tests

on:
  push:
    branches:
       - "main"
       - "dev/**"
       - "codex/**"
  pull_request:
    branches: ["main", "dev/**", "codex/**"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'maven'
      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
      - name: "Compile"
        run: mvn clean compile test-compile
      - name: "🧪 Java Unit Tests"
        run: mvn surefire:test@java-unit
      - name: "📷 Java Approval Tests"
        run: mvn surefire:test@java-approval
