INPUT:
        {
            a = 5;
            b = if a >= 0 then if a - 3 >= 0 then if a - 5 >= 0 then 1 else 2 else 3 else 4;
        }


PARSED AST:
brane'{
  integer'a = integer'5;
  b = if (a >= integer'0) then if ((a - integer'3) >= integer'0) then if ((a - integer'5) >= integer'0) then integer'1 else integer'2 else integer'3 else integer'4;
}

FINAL BRANE:
{
  integer'a = 5;
  integer'b = 1;
}

INITIAL FIROE:
FiroeProgram(FiroeBrane(
  FiroeAssignment(integer'a, Finear(5)),
  FiroeAssignment(b, FiroeIf(
    FiroeBinary(FiroeId(a), >=, Finear(0)),
    FiroeIf(
      FiroeBinary(FiroeBinary(FiroeId(a), -, Finear(3)), >=, Finear(0)),
      FiroeIf(
        FiroeBinary(FiroeBinary(FiroeId(a), -, Finear(5)), >=, Finear(0)),
        Finear(1),
        Finear(2)
      ),
      Finear(3)
    ),
    Finear(4)
  ))
))

EVALUATION RESULT:
FiroeBrane(
  FiroeAssignment(integer'a, Finear(5)),
  FiroeAssignment(integer'b, Finear(1))
)

FINAL BRANE STATUS:
FiroeBrane(
  FiroeAssignment(integer'a, Finear(5)),
  FiroeAssignment(integer'b, Finear(1))
)
