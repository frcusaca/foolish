INPUT:
        {
            {
                {
                    p = -5;
                    q = +10;
                };
                r = p + q;
            };
            s = r * 2;
        }


PARSED AST:
brane'{
  brane'{
    brane'{
      integer'p = -integer'5;
      integer'q = +integer'10;
    };
    integer'r = (p + q);
  };
  integer's = (r * integer'2);
}

FINAL BRANE:
{
  {
    {
      integer'p = -5;
      integer'q = 10;
    };
    integer'r = UNKNOWN;
  };
  integer's = UNKNOWN;
}

INITIAL FIROE:
FiroeProgram(FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'p, FiroeUnary(-, Finear(5))),
      FiroeAssignment(integer'q, FiroeUnary(+, Finear(10)))
    ),
    FiroeAssignment(integer'r, FiroeBinary(FiroeId(p), +, FiroeId(q)))
  ),
  FiroeAssignment(integer's, FiroeBinary(FiroeId(r), *, Finear(2)))
))

EVALUATION RESULT:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'p, Finear(-5)),
      FiroeAssignment(integer'q, Finear(10))
    ),
    FiroeAssignment(integer'r, Finear(UNKNOWN))
  ),
  FiroeAssignment(integer's, Finear(UNKNOWN))
)

FINAL BRANE STATUS:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'p, Finear(-5)),
      FiroeAssignment(integer'q, Finear(10))
    ),
    FiroeAssignment(integer'r, Finear(UNKNOWN))
  ),
  FiroeAssignment(integer's, Finear(UNKNOWN))
)
