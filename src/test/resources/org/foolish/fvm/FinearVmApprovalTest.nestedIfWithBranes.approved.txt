INPUT:
        {
            {
                {
                    x = if true then if true then 10 else 20 else if true then 30 else 40;
                };
            };
            y = x + 5;
        }


PARSED AST:
brane'{
  brane'{
    brane'{
      x = if boolean'true then if boolean'true then integer'10 else integer'20 else if boolean'true then integer'30 else integer'40;
    };
  };
  integer'y = (x + integer'5);
}

FINAL BRANE:
{
  {
    {
      integer'x = 10;
    };
  };
  integer'y = UNKNOWN;
}

INITIAL FIROE:
FiroeProgram(FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(x, FiroeIf(
        Finear(true),
        FiroeIf(
          Finear(true),
          Finear(10),
          Finear(20)
        ),
        FiroeIf(
          Finear(true),
          Finear(30),
          Finear(40)
        )
      ))
    )
  ),
  FiroeAssignment(integer'y, FiroeBinary(FiroeId(x), +, Finear(5)))
))

EVALUATION RESULT:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'x, Finear(10))
    )
  ),
  FiroeAssignment(integer'y, Finear(UNKNOWN))
)

FINAL BRANE STATUS:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'x, Finear(10))
    )
  ),
  FiroeAssignment(integer'y, Finear(UNKNOWN))
)
