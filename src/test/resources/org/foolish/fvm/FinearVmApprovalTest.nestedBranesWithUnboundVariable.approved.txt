INPUT:
        {
            {
                {
                    a = unknownVar;
                };
                b = a + 1;
            };
            c = b * 2;
        }


PARSED AST:
brane'{
  brane'{
    brane'{
      a = unknownVar;
    };
    integer'b = (a + integer'1);
  };
  integer'c = (b * integer'2);
}

FINAL BRANE:
{
  {
    {
      a = UNKNOWN;
    };
    integer'b = UNKNOWN;
  };
  integer'c = UNKNOWN;
}

INITIAL FIROE:
FiroeProgram(FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(a, FiroeId(unknownVar))
    ),
    FiroeAssignment(integer'b, FiroeBinary(FiroeId(a), +, Finear(1)))
  ),
  FiroeAssignment(integer'c, FiroeBinary(FiroeId(b), *, Finear(2)))
))

EVALUATION RESULT:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(a, Finear(UNKNOWN))
    ),
    FiroeAssignment(integer'b, Finear(UNKNOWN))
  ),
  FiroeAssignment(integer'c, Finear(UNKNOWN))
)

FINAL BRANE STATUS:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(a, Finear(UNKNOWN))
    ),
    FiroeAssignment(integer'b, Finear(UNKNOWN))
  ),
  FiroeAssignment(integer'c, Finear(UNKNOWN))
)
