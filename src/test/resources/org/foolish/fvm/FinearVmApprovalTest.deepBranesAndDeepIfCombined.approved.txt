INPUT:
        {
            {
                {
                    a = 2;
                };
                b = if a >= 0 then if a - 1 >= 0 then 100 else 200 else 300;
            };
            c = b + a;
        }


PARSED AST:
brane'{
  brane'{
    brane'{
      integer'a = integer'2;
    };
    b = if (a >= integer'0) then if ((a - integer'1) >= integer'0) then integer'100 else integer'200 else integer'300;
  };
  integer'c = (b + a);
}

FINAL BRANE:
{
  {
    {
      integer'a = 2;
    };
    integer'b = 300;
  };
  integer'c = UNKNOWN;
}

INITIAL FIROE:
FiroeProgram(FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'a, Finear(2))
    ),
    FiroeAssignment(b, FiroeIf(
      FiroeBinary(FiroeId(a), >=, Finear(0)),
      FiroeIf(
        FiroeBinary(FiroeBinary(FiroeId(a), -, Finear(1)), >=, Finear(0)),
        Finear(100),
        Finear(200)
      ),
      Finear(300)
    ))
  ),
  FiroeAssignment(integer'c, FiroeBinary(FiroeId(b), +, FiroeId(a)))
))

EVALUATION RESULT:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'a, Finear(2))
    ),
    FiroeAssignment(integer'b, Finear(300))
  ),
  FiroeAssignment(integer'c, Finear(UNKNOWN))
)

FINAL BRANE STATUS:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'a, Finear(2))
    ),
    FiroeAssignment(integer'b, Finear(300))
  ),
  FiroeAssignment(integer'c, Finear(UNKNOWN))
)
