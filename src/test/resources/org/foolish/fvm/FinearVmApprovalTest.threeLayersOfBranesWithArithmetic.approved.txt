INPUT:
        {
            {
                {
                    a = 1 + 2;
                };
                b = a * 3;
            };
            c = b - 4;
        }


PARSED AST:
brane'{
  brane'{
    brane'{
      integer'a = (integer'1 + integer'2);
    };
    integer'b = (a * integer'3);
  };
  integer'c = (b - integer'4);
}

FINAL BRANE:
{
  {
    {
      integer'a = 3;
    };
    integer'b = UNKNOWN;
  };
  integer'c = UNKNOWN;
}

INITIAL FIROE:
FiroeProgram(FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'a, FiroeBinary(Finear(1), +, Finear(2)))
    ),
    FiroeAssignment(integer'b, FiroeBinary(FiroeId(a), *, Finear(3)))
  ),
  FiroeAssignment(integer'c, FiroeBinary(FiroeId(b), -, Finear(4)))
))

EVALUATION RESULT:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'a, Finear(3))
    ),
    FiroeAssignment(integer'b, Finear(UNKNOWN))
  ),
  FiroeAssignment(integer'c, Finear(UNKNOWN))
)

FINAL BRANE STATUS:
FiroeBrane(
  FiroeBrane(
    FiroeBrane(
      FiroeAssignment(integer'a, Finear(3))
    ),
    FiroeAssignment(integer'b, Finear(UNKNOWN))
  ),
  FiroeAssignment(integer'c, Finear(UNKNOWN))
)
