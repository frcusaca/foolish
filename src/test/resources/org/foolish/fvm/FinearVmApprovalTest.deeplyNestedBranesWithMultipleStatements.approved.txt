INPUT:
        {
            a = 1;
            {
                b = 2;
                {
                    c = 3;
                    {
                        d = a + b + c;
                    };
                };
            };
            result = a + b + c + d;
        }


PARSED AST:
brane'{
  integer'a = integer'1;
  brane'{
    integer'b = integer'2;
    brane'{
      integer'c = integer'3;
      brane'{
        integer'd = ((a + b) + c);
      };
    };
  };
  integer'result = (((a + b) + c) + d);
}

FINAL BRANE:
{
  integer'a = 1;
  {
    integer'b = 2;
    {
      integer'c = 3;
      {
        integer'd = 6;
      };
    };
  };
  integer'result = UNKNOWN;
}

INITIAL FIROE:
FiroeProgram(FiroeBrane(
  FiroeAssignment(integer'a, Finear(1)),
  FiroeBrane(
    FiroeAssignment(integer'b, Finear(2)),
    FiroeBrane(
      FiroeAssignment(integer'c, Finear(3)),
      FiroeBrane(
        FiroeAssignment(integer'd, FiroeBinary(FiroeBinary(FiroeId(a), +, FiroeId(b)), +, FiroeId(c)))
      )
    )
  ),
  FiroeAssignment(integer'result, FiroeBinary(FiroeBinary(FiroeBinary(FiroeId(a), +, FiroeId(b)), +, FiroeId(c)), +, FiroeId(d)))
))

EVALUATION RESULT:
FiroeBrane(
  FiroeAssignment(integer'a, Finear(1)),
  FiroeBrane(
    FiroeAssignment(integer'b, Finear(2)),
    FiroeBrane(
      FiroeAssignment(integer'c, Finear(3)),
      FiroeBrane(
        FiroeAssignment(integer'd, Finear(6))
      )
    )
  ),
  FiroeAssignment(integer'result, Finear(UNKNOWN))
)

FINAL BRANE STATUS:
FiroeBrane(
  FiroeAssignment(integer'a, Finear(1)),
  FiroeBrane(
    FiroeAssignment(integer'b, Finear(2)),
    FiroeBrane(
      FiroeAssignment(integer'c, Finear(3)),
      FiroeBrane(
        FiroeAssignment(integer'd, Finear(6))
      )
    )
  ),
  FiroeAssignment(integer'result, Finear(UNKNOWN))
)
