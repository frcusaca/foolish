{
    ; Comments can be placed here
    ; These branes are the system symbols
    characterization = {}
    type = characterization'{};
    identifier = characterization'{}
    brane = type'{};
    integer = type'{};
    liberatedBrane = {parameter1;parameter2;}{result = parameter1 + parameter2}
}